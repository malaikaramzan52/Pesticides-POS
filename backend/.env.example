PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string (set this in Vercel / production env vars)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>

JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=30d
CORS_ORIGIN=*
